# Supabase (main app project) — copy to .env and fill in real values.
# Client-safe values only. NEVER put the service_role key in any VITE_ var.
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
