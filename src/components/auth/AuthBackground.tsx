/**
 * Retired in the design unification (Phase 4.1): the blurred glass blobs are gone.
 * Auth screens now sit on the warm-gray neomorphic base (body bg-background).
 * Kept as a no-op so the Auth.tsx call-site doesn't need to change.
 */
export function AuthBackground() {
  return null;
}
